import { AntiBatmanPipe } from './anti-batman.pipe';

describe('AntiBatmanPipe', () => {
  it('create an instance', () => {
    const pipe = new AntiBatmanPipe();
    expect(pipe).toBeTruthy();
  });
});
